/ TODO: i believe that there is mock for mysql db, use that for unit testing

func testDbConn() (db *sql.DB) {
    // TODO: use the mock to test the DB connection functionality
}

func testInsertRepos(db, values ) {
    // TODO: use the mock to test insert syntax and required marshal/unmarshal
}

fun getMaxRepoCreatedAt(db) (string) {
    // TODO: use the mock to test query syntax and required marshal/unmarshal
}
